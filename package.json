{
  "name": "pulse",
  "private": true,
  "version": "0.1.0",
  "description": "Real-time collaborative text editor with Yjs CRDT sync, Redis pub/sub fanout, and Groq-streamed AI suggestions.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/megradhikan/pulse.git"
  },
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "build": "pnpm --filter backend build && pnpm --filter frontend build",
    "test:concurrent": "pnpm --filter backend test:concurrent",
    "test:load": "pnpm --filter backend test:load"
  }
}
